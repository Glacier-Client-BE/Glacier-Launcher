<Window x:Class="MinecraftLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:pages="clr-namespace:MinecraftLauncher.Pages"
        xmlns:blazor="clr-namespace:Microsoft.AspNetCore.Components.WebView.Wpf;assembly=Microsoft.AspNetCore.Components.WebView.Wpf"
        Title="Glacier Launcher"
        Width="740" Height="500"
        MinWidth="500" MinHeight="360"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="ResizeGrip">
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <!-- Shadow -->
        <Border CornerRadius="16" Background="#1A1A20" ClipToBounds="False">
            <Border.Effect>
                <DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity="0.65" Color="Black"/>
            </Border.Effect>
        </Border>

        <!-- Clipped content -->
        <Border CornerRadius="16" ClipToBounds="True" Background="Transparent">
            <blazor:BlazorWebView x:Name="blazorWebView"
                                  HostPage="wwwroot/index.html"
                                  Services="{DynamicResource services}">
                <blazor:BlazorWebView.RootComponents>
                    <blazor:RootComponent Selector="#app"
                                          ComponentType="{x:Type pages:Home}"/>
                </blazor:BlazorWebView.RootComponents>
            </blazor:BlazorWebView>
        </Border>
    </Grid>
</Window>